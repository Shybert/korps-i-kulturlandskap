{% macro lead(title, text) %}
  <section id="lead" class="mainSection thin">
    <div class="container">
      <h1>{{ title }}</h1>
      {% markdown %}
        {{ text }}
      {% endmarkdown %}
    </div>
  </section>  
{% endmacro %}
